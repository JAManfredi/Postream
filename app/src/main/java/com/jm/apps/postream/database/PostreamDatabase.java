package com.jm.apps.postream.database;

import com.raizlabs.android.dbflow.annotation.Database;

@Database(name = PostreamDatabase.NAME, version = PostreamDatabase.VERSION)
public class PostreamDatabase {
    public static final String NAME = "PostreamDatabase";
    public static final int VERSION = 1;
}
